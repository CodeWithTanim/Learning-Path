// Arithmetic operators = + - * / %
// Assignment operators = =, +=, -=, *=, /=, %=
// Comparison Operators ==, !=, ===, !==, <, >, <=, >=
// Logical Operators &&, ||, !


const a = 10
const b = 3
const sum = a + b
console.log(sum)

const division = a / b
console.log(division.toFixed(2))

const reminder = a % b
console.log(reminder)

// a + b - (c * f) / e
// bracket > multiply > divide > add > subtract

// sum = sum + 10
// sum += 10

const c = a > b && 10
const d = a < b

console.log(c)
console.log(d)

const e = !(a > b) && 10
console.log(e)

